{
	"name": "mvccore/ext-tracy-routing",
	"description": "Extension for MvcCoreExt_Tracy extension to render and add into tracy debug panel all configured routes into current MvcCore application router and to display currently matched route.",
	"keywords": ["mvccore", "framework", "extension", "tracy", "routing", "active route", "router", "route", "current route"],
	"license": ["BSD-3-Clause"],
	"authors": [
		{
			"name": "Tom Flidr",
			"homepage": "https://github.com/tomFlidr"
		}
	],
	"require": {
		"php": ">=5.3.0",
		"mvccore/mvccore": "^3.0",
		"tracy/tracy": "^2.4",
		"mvccore/ext-tracy": "^3.0"
	},
	"autoload": {
		"classmap": ["src/"]
	},
	"minimum-stability": "dev"
}
